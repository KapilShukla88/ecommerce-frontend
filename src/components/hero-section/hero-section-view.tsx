import Carousal from "@components/common/carousal";
import React from "react";

const HeroSectionView: React.FC<{}> = () => {
  return (
    <div className="w-full">
      <Carousal />
    </div>
  );
};

export default HeroSectionView;
