// "use server";

const useTrendsComponentController = () => {
  const handleCallback = async () => {
    "use server";
    return null;
  };

  return { handleCallback };
};

export default useTrendsComponentController;
